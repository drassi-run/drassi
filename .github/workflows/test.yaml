name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        go-version: [1.23.x]
        platform: [ubuntu-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Install Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ matrix.go-version }}

      - name: Checkout code
        uses: actions/checkout@v3

      - name: Fmt
        run: "diff <(gofmt -d .) <(printf '')"
        shell: bash

#      - name: Vet
#        run: |
#          go vet ./core/...
#          go vet ./gha-runner/...

      - name: Test
        run: |
          go test -race ./core/...
          go test -race ./gha-runner/...
